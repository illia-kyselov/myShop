import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    images: {
        domains: [
            "example.com", 
            "images.pexels.com", 
            "i.pinimg.com",
            "images.unsplash.com",
            "i.pravatar.cc"
        ],
    },
};

export default nextConfig;
